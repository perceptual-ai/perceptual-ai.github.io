# Development

Serve `index.html` locally on http://localhost:8000:

```bash
pixi run preview
```

Publishing is automatic: GitHub Pages serves from the `main` branch, so any push to `main` deploys the site.
